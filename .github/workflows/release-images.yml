name: Release Images

on:
  release:
    types: [ published ]

jobs:
  agent:
    uses: galam/pinpoint-docker/.github/workflows/docker-image.yml@garamrelease
    with:
      component: pinpoint-agent
    secrets:
      docker_username: ${{ secrets.DOCKER_USERNAME }}
      docker_token: ${{ secrets.DOCKER_TOKEN }}
      docker_password: ${{ secrets.DOCKER_PASSWORD }}

  collector:
    uses: ga-ram/pinpoint-docker/.github/workflows/docker-image.yml@garamrelease
    with:
      component: pinpoint-collector
    secrets:
      docker_username: ${{ secrets.DOCKER_USERNAME }}
      docker_token: ${{ secrets.DOCKER_TOKEN }}
      docker_password: ${{ secrets.DOCKER_PASSWORD }}
          
  flink:
    uses: ga-ram/pinpoint-docker/.github/workflows/docker-image.yml@garamrelease
    with:
      component: pinpoint-flink
    secrets:
      docker_username: ${{ secrets.DOCKER_USERNAME }}
      docker_token: ${{ secrets.DOCKER_TOKEN }}
      docker_password: ${{ secrets.DOCKER_PASSWORD }}
           
  hbase:
    uses: ga-ram/pinpoint-docker/.github/workflows/docker-image.yml@garamrelease
    with:
      component: pinpoint-hbase
    secrets:
      docker_username: ${{ secrets.DOCKER_USERNAME }}
      docker_token: ${{ secrets.DOCKER_TOKEN }}
      docker_password: ${{ secrets.DOCKER_PASSWORD }}
          
  mysql:
    uses: ga-ram/pinpoint-docker/.github/workflows/docker-image.yml@garamrelease
    with:
      component: pinpoint-mysql
    secrets:
      docker_username: ${{ secrets.DOCKER_USERNAME }}
      docker_token: ${{ secrets.DOCKER_TOKEN }}
      docker_password: ${{ secrets.DOCKER_PASSWORD }}
          
  quickstart:
    uses: ga-ram/pinpoint-docker/.github/workflows/docker-image.yml@garamrelease
    with:
      component: pinpoint-quickstart
    secrets:
      docker_username: ${{ secrets.DOCKER_USERNAME }}
      docker_token: ${{ secrets.DOCKER_TOKEN }}
      docker_password: ${{ secrets.DOCKER_PASSWORD }}
          
  web:
    uses: ga-ram/pinpoint-docker/.github/workflows/docker-image.yml@garamrelease
    with:
      component: pinpoint-web
    secrets:
      docker_username: ${{ secrets.DOCKER_USERNAME }}
      docker_token: ${{ secrets.DOCKER_TOKEN }}
      docker_password: ${{ secrets.DOCKER_PASSWORD }}
          
  batch:
    uses: ga-ram/pinpoint-docker/.github/workflows/docker-image.yml@garamrelease
    with:
      component: pinpoint-batch
    secrets:
      docker_username: ${{ secrets.DOCKER_USERNAME }}
      docker_token: ${{ secrets.DOCKER_TOKEN }}
      docker_password: ${{ secrets.DOCKER_PASSWORD }}
